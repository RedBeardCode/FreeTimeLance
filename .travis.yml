language: python
python:
  - "2.7"
  - "3.5"

services: postgresql

cache: pip

env:
  global:
    - DJANGO_SETTINGS_MODULE=FreeTimeLance.test_settings
  matrix:
    - SAUCE_BROWSER="safari" SAUCE_PLATFORM="OS X 10.11"
    - SAUCE_BROWSER="MicrosoftEdge" SAUCE_PLATFORM="Windows 10"
    - SAUCE_BROWSER="internet explorer" SAUCE_PLATFORM="Windows 10"
    - SAUCE_BROWSER="chrome" SAUCE_PLATFORM="Windows 10"
    - SAUCE_BROWSER="chrome" SAUCE_PLATFORM="Linux"
    - SAUCE_BROWSER="firefox" SAUCE_PLATFORM="Windows 10"
    - SAUCE_BROWSER="firefox" SAUCE_PLATFORM="Linux"



install:
  - pip install -r requirements.txt
  - pip install -r requirements_dev.txt
  - pip install python-coveralls
  - pip install pylama

script:
  - python manage.py makemigrations project
  - python manage.py migrate
  - py.test  --pylama --splinter-webdriver='remote' --cov-config .coveragerc --cov=project --cov=FreeTimeLance --splinter-make-screenshot-on-failure=false

addons:
  sauce_connect: true

after_success:
  - coveralls
